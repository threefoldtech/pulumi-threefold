name: pulumi-provider-grid
runtime: yaml

plugins:
  providers:
    - name: grid
      path: ../..

resources:
  provider:
    type: pulumi:providers:grid
    properties:
      mnemonic:

  fqdnGateway:
    type: grid:internal:FqdnGateway
    options:
      provider: ${provider}
    properties:
      name: gateway
      node_id: 3
      fqdn: test.gent01.qa.grid.tf
      backends:
        - http://69.164.223.208
